readlines method used to read files as list whereas read() does it as strings

list(todo_list)
Enumerate can be used too
        for index , list in enumerate(todo_list):
            print(f"{index+1}) {list}")
open(f"../files/{filename}","w")to leave the current folder and go to another folder in same drive

//////////////////////////////////////////////////////////
/// new_list = []                                      ///
/// for item in old_list:                              ///
///     new_item = item.strip("\n")                    ///
///     new_list.append(new_item)                      ///
///                or                                  ///
/// new_list = [item.strip("\n") for item in old_list] ///
///                                                    ///
//////////////////////////////////////////////////////////

                           TO CHECK EXISTING FILE IF NOT CREATING ONE

import os

file_name = "coinflip.txt"
if not os.path.exists(file_name):
    with open(f"{file_name}", "w") as file:
        file.close()

with open(f"{file_name}", "r") as file:
    toss_list = file.readlines()

//////////////////////////////////////////////////////////////

""" string """(doc string)
when you can create multiple Python files for one program and then these files can interact with each other.Then you might want to access the help documentation of a function from another file.And so then it becomes helpful to have this docstring as an experiment.

//////////////////////////////////////////////////////////////

Modules::
from "filename" import "specific function names"
or
import "filename"
access the functions in the module like filename.function_name()